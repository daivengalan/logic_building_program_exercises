# Ask the user for two numbers 
dividend_value = float(input("Enter the dividend: "))
divisor_value = float(input("Enter the divisor: "))
# Calculate the remainder using the modulo operator (%)
remainder_result = dividend_value % divisor_value
# Print the final result
print(f"The remainder of the division is: {remainder_result}")